spring:
  application:
    name: codelab-gateway
  cloud:
    config:
      uri: http://codelab-config:20088
      fail-fast: true
      password: ${CODELAB_PASS}
      username: ${CODELAB_USER}
